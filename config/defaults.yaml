defaults:
  run_name: none
  input_dir: ./data
  log_interval: 500
  save_path: ./log/model.pt
  save_interval: 10_000

  # Data
  data_seq: True

  # Training
  n_steps: 100_000
  batch_length: 15
  batch_size: 15
  device: 'cuda:0'

  # Model
  embed_dim: 256
  deter_dim: 200
  stoch_dim: 10
  hidden_dim: 200

  # Optimizer
  grad_clip:
